<li>
	{{dataValues.burger_name}}

	<button class="change-state btn-sm" data-id="{{dataValues.id}}" data-newstate="{{dataValues.devoured}}">
		{{#if dataValues.devoured}}COOK!{{else}}EAT!{{/if}}
	</button>

</li>
